class Windows extends Items {
    Windows(String windowsPath, int x, int y, int width, int height, boolean collsion, boolean interactable) {
      super(windowsPath, x, y, width, height, collsion, interactable);
    }
}
