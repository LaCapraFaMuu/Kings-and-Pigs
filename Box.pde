class Box extends Items {
    Box(String boxPath, int x, int y, int width, int height, boolean collsion, boolean interactable) {
      super(boxPath, x, y, width, height, collsion, interactable);
    }
}
